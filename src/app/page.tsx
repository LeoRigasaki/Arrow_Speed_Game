import React from 'react';
import ArrowKeySpeedTest from '@/components/ui/ArrowKeySpeedTest';

const HomePage: React.FC = () => {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <ArrowKeySpeedTest />
    </main>
  );
};

export default HomePage;
